# 腾讯云服务器更新部署说明

## 当前状态
✓ 生产版本已构建完成（dist/ 文件夹，274MB）
✓ 包含 Hole in One 游戏完整中文汉化
✓ 新增 10 个关卡（16-25）使用所有游戏元素

## 服务器信息
- 服务器IP: 119.29.84.36
- 网站地址: http://119.29.84.36

## 部署方式（选择其一）

### 方式一：使用 WinSCP 或 FileZilla（推荐，最简单）

1. **下载工具**
   - WinSCP: https://winscp.net/
   - FileZilla: https://filezilla-project.org/

2. **连接服务器**
   - 协议: SFTP
   - 主机: 119.29.84.36
   - 用户名: root
   - 密码: [你的服务器密码]
   - 端口: 22

3. **上传文件**
   - 找到服务器上的网站目录（可能是 /root/artislife/dist 或类似路径）
   - 删除服务器上旧的 dist 文件夹内容
   - 上传本地 D:\youhua\6541\dist\ 文件夹中的所有内容

4. **重启服务**（如果需要）
   ```bash
   pm2 restart all
   # 或
   systemctl restart nginx
   ```

### 方式二：使用命令行 SCP

```bash
# 压缩 dist 文件夹
cd /d/youhua/6541
tar -czf dist.tar.gz dist/

# 上传到服务器（需要输入密码）
scp dist.tar.gz root@119.29.84.36:/root/

# 登录服务器解压
ssh root@119.29.84.36
cd /root/artislife  # 或你的实际路径
rm -rf dist/*
tar -xzf /root/dist.tar.gz
mv dist/* dist/
rm /root/dist.tar.gz
pm2 restart all  # 重启服务
```

### 方式三：使用 Git 部署

如果服务器上有 Git 仓库：

```bash
# 本地提交更改
cd /d/youhua/6541
git add .
git commit -m "Update: Hole in One game Chinese localization + 10 new levels"
git push origin main

# 登录服务器拉取更新
ssh root@119.29.84.36
cd /root/artislife  # 或你的实际路径
git pull origin main
npm run build
pm2 restart all
```

### 方式四：使用腾讯云 COS 对象存储

1. 登录腾讯云控制台: https://console.cloud.tencent.com/cos
2. 找到你的存储桶
3. 删除旧文件
4. 上传 dist/ 文件夹中的所有内容
5. 清除 CDN 缓存（如果使用了 CDN）

## 验证部署

部署完成后，访问以下页面测试：

1. 主页: http://119.29.84.36
2. 游戏页面: http://119.29.84.36 → 点击"游戏"
3. Hole in One: 进入游戏 → 选择"趣味游戏" → "一杆进洞"
4. 测试新关卡: 玩到第16关，检查是否为"机器人迷宫"

## 需要更新的文件

主要更新的文件包括：
- components/holeinone/App.tsx（中文汉化）
- components/holeinone/levels.ts（10个新关卡）
- components/HoleInOneGame.tsx（新增）
- components/ArtGame.tsx（集成 Hole in One）

## 故障排查

如果部署后游戏无法运行：
1. 清除浏览器缓存（Ctrl+Shift+Delete）
2. 检查浏览器控制台是否有错误
3. 确认服务器上的文件已完全更新
4. 重启服务器服务（pm2 restart all）

## 需要帮助？

如果遇到问题，请告诉我：
1. 你使用的是哪种部署方式
2. 具体的错误信息
3. 服务器上的实际目录结构
